// schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = env("POSTGRES_PRISMA_URL") // uses connection pooling
  directUrl = env("POSTGRES_URL_NON_POOLING") // uses a direct connection
}

model TransaksiBulanan {
    id            String       @default(cuid()) @id
    nama_obat     String?
    pemakaian     Int?
    penerimaan    Int?
    sisa_stok     Int?
    bulan         Int?
    tahun         Int?
    createdAt     DateTime  @default(now()) @map(name: "created_at")
    updatedAt     DateTime  @updatedAt @map(name: "updated_at")
    @@map(name: "transaksi_bulanan")
}

model ObatAlat {
    id            String       @default(cuid()) @id
    nama_obat     String?
    jenis_obat    String?
    createdAt     DateTime  @default(now()) @map(name: "created_at")
    updatedAt     DateTime  @updatedAt @map(name: "updated_at")
    @@map(name: "obat_alat")
}

model HasilPengadaan {
    id            String       @default(cuid()) @id
    nama_obat     String?
    jumlah        Int?
    @@map(name: "hasil_pengadaan")
}

model User {
    id            String       @default(cuid()) @id
    name          String?
    username      String?      @unique
    email         String?   @unique
    createdAt     DateTime  @default(now()) @map(name: "created_at")
    updatedAt     DateTime  @updatedAt @map(name: "updated_at")
    @@map(name: "users")
}